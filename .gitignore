# Shared engineering knowledge is committed; only personal Obsidian UI state is local.
/Obsidian-Fastwork/.obsidian/

# env files (secrets) — anywhere in the monorepo
.env
.env.*
!.env.example

# dependencies / build artifacts
node_modules/
dist/

# Playwright MCP session artifacts (screenshots, snapshots, console logs)
/.playwright-mcp/
.firecrawl/

# Local personal MCP server config (Hostinger domain/DNS tooling; token via env)
.mcp.json
